# tests/ci_portable.txt — the gates a CLEAN CHECKOUT can run (no inputs).
# THE JOB MUST FAIL ON SKIP under --strict: a run of 3% of the gates that
# reports green is its own false green.
g_pass
g_skip
g_skip_indent
g_prose
g_segv_prose
g_hygiene
# g_control: a gate under the must-fire contract — declared, fired, executable
g_control
